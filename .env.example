# ============================================================================
# 《天梦大陆》云存档环境变量模板（TM-P2-005）
# ============================================================================
# 用法：
#   本文件是占位模板，复制为本地文件再填真实值。推荐命名 .env.local
#   （已被 .gitignore 的 `*.local` 规则忽略）。注意：本仓库 .gitignore
#   并【没有】忽略 `.env`，若使用 `.env` 请自行确认它不被 git 跟踪/提交。
#
# ⚠️ 安全红线（务必遵守）：
#   - 真实值只允许存在于：本地未提交的 .env.local，以及 Supabase 平台的
#     secrets（Edge Function 环境变量）。两者都绝不 commit。
#   - VAULT_HMAC_SECRET / SUPABASE_SERVICE_ROLE_KEY 是服务器端机密：
#     · 绝不写进任何 VITE_* 变量（VITE_* 会被 Vite 打包进前端 bundle，
#       随 GitHub Pages 对全网公开）；
#     · 绝不写进 dist/ 构建产物；
#     · 绝不写进 README / 文档 / 截图 / 公开仓库的任何文件。
#   - VITE_CLOUD_SAVE_ENDPOINT 是唯一需要暴露给前端的值（Edge Function 公网 URL），
#     它本身不含机密，仅用于前端 fetch。

# 前端唯一公开配置（构建时注入；生产填 Edge Function 公网 URL）：
#   例如 https://<project-ref>.functions.supabase.co/cloud-save
#   留空 = 前端判定云层未配置，进入仅本机模式（页面会提示云存档服务尚未配置）。
VITE_CLOUD_SAVE_ENDPOINT=

# 服务器端机密（仅用于 Supabase Edge Function 的 secrets；
# 本地调试时可用 supabase functions serve 读取同目录 .env.local）：
#   VAULT_HMAC_SECRET：派生 vault_id 的 HMAC-SHA256 密钥（任意足够长的随机串，
#     建议 32+ 字节随机值；一经上线不可随意更换，否则旧口令将找不到 vault）。
#   SUPABASE_URL：项目 REST URL（如 https://xxxx.supabase.co）。
#   SUPABASE_SERVICE_ROLE_KEY：service role key（Dashboard → Settings → API）。
VAULT_HMAC_SECRET=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
VITE_CLOUD_SAVE_API_BASE=
